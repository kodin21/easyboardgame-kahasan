const maxLimitX = 500;
const maxLimitY = 500;

const normalSpeed = 10;
const fastSpeed = 50;

const charHeight = 18;
const charWidth = 18;

export {
  maxLimitX, maxLimitY, normalSpeed, fastSpeed, charHeight, charWidth,
};
